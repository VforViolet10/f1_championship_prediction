# -*- coding: utf-8 -*-
"""lca1_nn.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1OESE_KRbicKxmiUsWMtL2sQ7D5sXVHEK
"""

!pip install --upgrade kagglehub

import pandas as pd
import keras
from keras.models import Sequential
from keras.layers import Dense
import os
import pandas as pd
import kagglehub
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler

import kagglehub
path = kagglehub.dataset_download("rohanrao/formula-1-world-championship-1950-2020")

import os

path = "/kaggle/input/formula-1-world-championship-1950-2020"
os.listdir(path)

csv_file_path=os.path.join(path,'driver_standings.csv')
data=pd.read_csv(csv_file_path)

data.head()

#split into input (x) and output (y) variables
x=data.iloc[:,0:6]
y=data.iloc[:,6]

x.dtypes

x = x.select_dtypes(include=['int64', 'float64'])

#Train-test split
x_train, x_test, y_train, y_test = train_test_split(
    x, y, test_size=0.2, random_state=42
    )

#Feature scaling
scaler = StandardScaler()
x_train = scaler.fit_transform(x_train)
x_test = scaler.transform(x_test)

model = Sequential()
model.add(Dense(16, activation='relu', input_shape=(x.shape[1],)))
model.add(Dense(8, activation='relu'))
model.add(Dense(1, activation='sigmoid'))

#compile the keras model
model.compile(loss='binary_crossentropy',optimizer='adam',metrics=['accuracy'])

#Train the model
history=model.fit(
    x_train,
    y_train,
    epochs=100,
    batch_size=16,
    validation_split=0.1,
    verbose=1
)

#Evaluate the model
loss, accuracy = model.evaluate(x_test, y_test)
print(f"Test Accuracy: {accuracy * 100:.2f}%")

#Make predictions
predictions = (model.predict(x_test)>0.5).astype(int)

y_test = y_test.reset_index(drop=True)

y_test_array = y_test.values

y_pred = (model.predict(x_test) > 0.5).astype(int).flatten()

#prediction
print("Prediction:     ", y_pred[:30])
print("Actual values: ", y_test_array[:30])